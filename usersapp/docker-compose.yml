version: '3.9'

services:
  usersapp:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: usersapp
    ports:
      - "80:80"
    restart: always
    environment:
      - NODE_ENV=production
