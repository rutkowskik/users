apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: users-backend-hpa
  namespace: users-app
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: users-backend
  minReplicas: 2
  maxReplicas: 6
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 75