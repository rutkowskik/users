version: "3.9"

services:
  users-app:
    build: .
    container_name: users-app
    image: ${FULL_IMAGE}
    ports:
      - "8080:8080"
    environment:
      - JAVA_OPTS=-Xms512m -Xmx1024m
    restart: unless-stopped